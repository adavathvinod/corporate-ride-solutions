Options -MultiViews
RewriteEngine On

# Force HTTPS + www
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} !^www\.adityatrav\.com$ [NC]
RewriteRule ^ https://www.adityatrav.com%{REQUEST_URI} [L,R=301]

# SPA fallback (React / Angular / Vue)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
